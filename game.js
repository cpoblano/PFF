const canvas = document.getElementById("gameCanvas");
const ctx = canvas.getContext("2d");

// Clear canvas with a color
ctx.fillStyle = "#000000"; // Change this to your color
ctx.fillRect(0, 0, canvas.width, canvas.height);