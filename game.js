const canvas = document.getElementById("Game");
const ctx = canvas.getContext("2d");

// Clear canvas with a color
ctx.fillStyle = "#042469"; // Change this to your color
ctx.fillRect(0, 0, canvas.width, canvas.height);